[
  {erlcloud, [
      {aws_config, [
          {access_key_id, "test"},
          {secret_access_key, "test"},
          {aws_region, "us-east-1"},
          {sns_host, "localhost"},
          {sns_scheme, "http://"},
          {sns_port, 4566},
          {sqs_host, "localhost"},
          {sqs_protocol, "http://"},
          {sqs_port, 4566}
      ]}
  ]}
].
